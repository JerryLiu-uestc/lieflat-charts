interface:
  display_name: "Lieflat Charts"
  short_description: "Editorial charts with Mono and curated color palettes"
  default_prompt: "Use $lieflat-charts to turn this dataset into a polished editorial chart, keeping Mono by default and using a curated palette only when I explicitly request color."
